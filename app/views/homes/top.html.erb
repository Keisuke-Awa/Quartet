<div>
<h2>アプリ</h2>
<p>いますぐセッティングできる</p>
<% if user_signed_in? %>
  <%= link_to('探す', home_user_path(current_user.id), class: "btn btn-lg btn-outline-success") %>
<% else %>
  <button type="button" class="btn btn-outline-success" data-toggle="modal" 
                                          data-target="#signupAuthModal">さっそく始める</button>
<% end %>
 <%= render "users/registrations/auth_select_modal" %>
</div>

