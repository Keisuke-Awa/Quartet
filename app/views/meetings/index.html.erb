<h3>プラン検索</h3>
<%= render 'search_form' %>

<% @meetings.each do |meeting| %>
  <% unless current_user?(meeting.planning_user) %>
    <%= render meeting %>
  <% end %>
<% end %>
<%= paginate @meetings, remote: true %>
